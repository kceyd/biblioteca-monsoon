version: '3.8'

services:
  biblioteca-service:
    container_name: biblioteca-contenedor
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: always